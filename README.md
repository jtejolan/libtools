# Library Tools

A cozy home for practical library software. The FastAPI service serves the
project homepage at `/`, the interactive API documentation at `/docs`, and the
Lendery endpoints under `/lendery`.

## Run locally

```sh
cd backend/app
uvicorn main:app --reload
```
